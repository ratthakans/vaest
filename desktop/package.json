{
  "name": "vaest-desktop",
  "version": "3.0.0",
  "private": true,
  "description": "VÆST desktop shell (Tauri) — a native window onto the live app",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
